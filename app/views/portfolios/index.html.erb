<h1>Portfolio Items</h1>

<%= link_to "Create New Item", new_portfolio_url if logged_in?(:site_admin) %>

<%= render partial: 'portfolio_item', collection: @portfolio_item %>

<hr>

<%= link_to "Angular", angular_items_path %>

